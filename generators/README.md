# generators/

Custom generators that run over an IP-XACT design — e.g. an address-map
generator for the crossbar, or a top-level wrapper generator. Empty for now;
Kactus2's built-in Verilog/SV generator covers the initial flow.
